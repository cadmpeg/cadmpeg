# FreeCAD `.FCStd`: Open Items

## Legacy persistence

- The complete object and property grammars for schema versions 2 and 3 are undefined.
- The dispatch boundaries among pre-schema-4 object layouts and property encodings are undefined.

## B-rep records

- The complete version table for text B-rep record branches in the primary envelope is undefined.
- The complete version table and field grammar for binary B-rep records in the primary envelope is
  undefined.
- The byte grammars of persistent element maps and string-hasher tables are undefined.

## Auxiliary records

- The framing and value grammars of application-specific side entries not referenced by a typed
  file property are undefined.
- The complete GUI property-type table and the persisted precedence among object, subshape, and
  material appearance records are undefined.
