# SPDX-License-Identifier: Apache-2.0
# Deliberately broken layout table: a field cites a phrase the specification does not contain
# `crates/cadmpeg/tests/layout_tables.rs` asserts the validator rejects it.
schema = 1
format = "f3d"
spec = "docs/formats/f3d.md"
endianness = "little"

[[record]]
name = "asmheader_binaryfile8"
kind = "byte"
section = "3"
anchor = "`BinaryFile8` header layout"

[[record.field]]
name = "secret_checksum"
offset = 0
type = "u32"
source = "spec"
anchor = "`0..4` | little-endian u32 payload checksum"
