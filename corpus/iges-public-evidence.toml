schema_version = 1
fixtures = []

# Each [[fixtures]] entry names one committed IGES corpus filename, one or more
# fixture_classes, every assertions label it verifies, exact codec tests that
# load the file and enforce those assertions, and SHA-256 digests for its
# serialized inspection, canonical IR, and decode report.
